"""Chunking — the ``Chunker`` interface and its implementations.

``base.py`` defines the ABC, ``structural.py`` the JSON structural chunker,
and ``strategies/`` the seven comparison strategies. All implement the same
interface so they are interchangeable in the comparator.
"""
